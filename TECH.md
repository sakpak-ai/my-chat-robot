 
一、技术路线
 
1. 开发语言与核心库
 
- Python 3.9+：程序主体开发语言
- Streamlit：快速搭建交互式网页前端，无需前端框架
- requests：发送 HTTP POST 请求，对接各大模型开放API
- pandas：结构化存储接口性能指标，生成表格数据
- time：时间戳计算，统计接口响应耗时
 
2. 接口标准规范
 
统一采用 OpenAI 兼容式接口格式鉴权与传参：
 
1. 请求头鉴权： Authorization: Bearer {API密钥} 
2. 请求体固定字段：模型名称、用户对话内容、随机度 temperature
3. 统一接收返回  choices  数组内的模型回答文本
 
3. 整体架构分层
 
1. 配置层：MODEL_CONFIG 字典，统一管理所有模型接口信息；
2. 接口封装层：call_ai_model 通用请求函数，负责网络请求、计时、异常处理；
3. 页面交互层：Streamlit UI 组件，提供选择、输入、触发按钮；
4. 数据可视化层：Pandas 表格 + Streamlit 柱状图，展示性能对比数据。
 
二、核心代码说明
 
1. 模型配置模块
 
python
  
MODEL_CONFIG = {
    "模型名称": {
        "api_url": "接口地址",
        "api_key": "鉴权密钥",
        "model_name": "平台内部模型标识"
    }
}
 
 
作用：集中管理所有模型接口信息，解耦接口硬编码；新增模型仅需在此字典添加配置，无需修改请求逻辑。
 
2. 通用接口调用函数 call_ai_model
 
1. 密钥预处理： real_api_key = config["api_key"].strip() ，自动清除密钥前后空格，避免空白字符导致鉴权401；
2. 请求参数封装：标准化 headers、request_data，适配三家大模型兼容接口；
3. 超时防护： timeout=30 ，防止校园网、弱网环境请求无限阻塞；
4. 计时逻辑：请求前后记录时间戳，计算接口整体耗时；
5. 结果解析：提取模型返回对话文本，计算输出字符长度、输出速度；
6. 全局异常捕获：捕获网络错误、鉴权错误、接口服务错误，返回带失败信息的指标，保证程序不崩溃。
 
3. 页面交互与数据可视化
 
1.  st.multiselect ：实现多模型勾选功能；
2.  st.text_area ：多行输入提问内容；
3. 循环遍历选中模型，逐个调用接口并展示回答；
4.  pd.DataFrame  将性能指标数组转为表格；
5.  st.bar_chart  绑定表格数据，自动生成可视化对比柱状图。
 
三、开发遇到的问题及解决方案
 
问题1：API密钥填写无误，但所有模型均返回401 Unauthorized鉴权失败
 
原因分析：
 
1. 智谱旧版密钥格式不支持 Bearer 鉴权，需要平台新建 sk 开头新版密钥；
2. 通义千问填入主账号 AccessKey，兼容接口需百炼平台专用 sk 密钥；
3. 密钥复制时携带首尾空白字符，服务端识别密钥异常。
解决方案：
4. 所有平台重新生成以  sk-  开头的标准密钥；
5. 代码增加  .strip()  自动清除密钥前后空格；
6. 核对接口地址、Authorization 头  Bearer  后必须保留空格。
 
问题2：终端打印接口返回内容时，抛出 latin-1 中文编码报错
 
原因分析：Windows 默认终端编码不支持中文，打印中文返回内容触发编码异常。
解决方案：注释代码中  print(response.text)  打印语句，规避终端中文输出，仅保留网页展示内容。
 
问题3：直接使用 python DAIMA.py 运行代码，提示 missing ScriptRunContext 警告
 
原因分析：Streamlit 程序必须通过  streamlit run  指令启动，普通Python运行方式缺少网页运行上下文。
解决方案：统一使用终端  streamlit run DAIMA.py  启动项目。
 
问题4：接口请求网络超时，程序卡死无反馈
 
原因分析：弱网/校园网环境请求无时间限制，长时间阻塞线程。
解决方案：requests.post 增加  timeout=30  参数，30秒无响应自动抛出异常并标记失败。
 
问题5：输出字符长度为0、响应时间接近0时，除法计算输出速度出现除零错误
 
原因分析：耗时为0时  len(result)/cost_time  触发数学报错。
解决方案：增加判断  speed = round(char_count / cost_time, 2) if cost_time > 0 else 0 ，避免除零异常。
 
问题6：硬编码API密钥存在泄露风险，不适合提交代码仓库
 
原因分析：密钥明文写在代码中，上传GitHub会被盗刷额度。
解决方案：课程演示本地运行可直填密钥；正式项目改用  .env  环境变量文件，搭配  .gitignore  屏蔽密钥文件不上传仓库。